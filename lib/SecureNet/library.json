{
  "name": "SecureNet",
  "version": "1.0.0",
  "description": "wolfSSL-backed TLS 1.3 client (SecureClient) + minimal keep-alive HTTP/1.1 (SecureHttpClient). Vendored from Free-Ink/freeink-sdk (MIT); adapted from crosspoint-reader PR #2475. Verifies against a curated root set (CrossPointRoots.h).",
  "frameworks": "arduino",
  "platforms": "espressif32",
  "build": {
    "srcDir": "src",
    "includeDir": "include"
  }
}
